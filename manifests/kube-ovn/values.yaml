# Kube-OVN Helm chart values for k3k virtual cluster
# Chart: kubeovn/kube-ovn v1.16.2
# Repo: https://kubeovn.github.io/kube-ovn/

networking:
  POD_CIDR: "10.16.0.0/16"
  SVC_CIDR: "10.96.0.0/12"
  JOIN_CIDR: "100.64.0.0/16"
  TUNNEL_TYPE: "geneve"
  # Leave IFACE empty for auto-detection; set explicitly if needed
  # IFACE: "eth0"
